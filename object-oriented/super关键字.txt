1.super不是引用类型，super中存储的不是内存地址，super指向的不是父类对象，'指向的是父类型特征'。
2.子类和父类中都有某个数据，要在子类中访问父类的这个数据，需要使用super


1.可以用在成员方法中，不能用在静态方法中
2.可以用在构造方法中
super(实参) 通过子类的构造方法去调用父类的构造方法，并不创建父类对象
super()的调用只能放在构造方法的第一行

