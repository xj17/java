1.toString返回该对象的字符串表示，重写toString方法
//print方法后面括号如果是一个引用类型，会默认调用引用类型的toString方法
//类名@java对象（堆中）的内存地址经过哈希算法得出的int类型转换成16进制

2.equals方法
设计目的，判断两个对象是否一样

3.finalize方法   垃圾回收器调用 该对象马上就要被回收了 需要释放资源
（垃圾回收器在回收这个JAVA对象之前会自动调用该对象的finalize方法）

